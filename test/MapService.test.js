define(['../lib/MapService'], function (MapService) {

    describe("initial test", function () {
        it("should work", function () {
            console.log('The MapService looks like:');
            console.log(MapService);
            // const mapService = new MapService();
            // const serviceAddress = 'https://worldwind25.arc.nasa.gov/wms';
            // const url = mapService.buildWmsUrl(serviceAddress);
            fail("do something");
            // expect(url).toBe('https://worldwind25.arc.nasa.gov/wms?service=WMS&request=GetCapabilities');
        });
    });
});
